name: Auto-approve on PRs

on:
  pull_request_target:
    types:
      - opened

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
      - name: Auto-approve PR
        uses: hmarr/auto-approve-action@v4
